---
import ArticleCard from '@/components/ArticleCard'
import { cn } from '@/utils'
import type { Article } from '@/utils/articles'

interface Props {
	articles: Article[]
	FirstBig?: boolean
	eagerCount?: number
}

const { articles, FirstBig = false, eagerCount = 0 } = Astro.props
---

<section
	class={cn(
		`grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-8 mt-3`,
		FirstBig && `md:[&>*:first-child]:col-span-2`
	)}
>
	{articles.map((article, i) => <ArticleCard article={article} eager={i < eagerCount} />)}
</section>
